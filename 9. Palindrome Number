class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        if x < 0:
            return False

        string_x = str(x)
        return string_x == string_x[::-1]

x = -121
solution = Solution()
print(solution.isPalindrome(x))
